You are explicitly in `/home/matan/dev-projects/upside`, in WSL.

See [AGENTS.md](AGENTS.md) for the shared agent collaboration protocol.

The product spec lives in [`spec/`](spec/README.md) — start at the README for the file map (root files + `spec/signals/` + `spec/screens/`). Don't read the whole spec on every turn; pull only the files relevant to the work in front of you. Before editing any spec file or persisting a design decision, invoke the `spec-edit` skill.